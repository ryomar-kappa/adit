name: adit
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  auto_route: ^9.2.2
  build_runner: ^2.4.12
  flutter:
    sdk: flutter
  hooks_riverpod: ^2.5.2

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
